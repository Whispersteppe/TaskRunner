<UserControl x:Class="TaskRunner.View.FolderEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskRunner.View"
             mc:Ignorable="d" Height="125" Width="262">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" MinWidth="50" />
            <ColumnDefinition Width="*"  />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <Label Content="Folder" Grid.Row="0" Grid.Column="0" />

        <Label Content="Name" Grid.Row="1" Grid.Column="0" />
        <TextBox Text="{Binding Path=Name}"  HorizontalAlignment="Left" MinWidth="100" Grid.Row="1" Grid.Column="1" />

    </Grid>
</UserControl>
